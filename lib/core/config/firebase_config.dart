// Firebase configuration placeholder
//
// This file intentionally contains no active Firebase configuration so the app
// can be built and tested without requiring firebase_options.dart. When you
// are ready to enable Firebase, generate `firebase_options.dart` using the
// FlutterFire CLI (https://firebase.flutter.dev/docs/cli) and replace this
// placeholder with a proper `FirebaseConfig` or import the generated
// `firebase_options.dart` and call `Firebase.initializeApp()` in `main.dart`.

// Example (do not uncomment until firebase_options.dart exists):
// import 'package:firebase_core/firebase_core.dart';
// import 'firebase_options.dart';
// await Firebase.initializeApp(options: DefaultFirebaseOptions.currentPlatform);

